Chatroomlist:
=============

add ip <ip>
add host <hostname>
set (send|check)? null (client|server)? [01]
set deny [01]
create <chatroomname>
close <chatroomid> (<reason>)?

Chatroom:
=========

to send "/<msg>" write "//<msg>"

/set status (online|away|busy) (<reason>)?
/kick <uid> (<reason>)?
/set (kick|mod) [01] <uid> (<reason>)?
/quit (<reason>)?
/<uid> <message>
